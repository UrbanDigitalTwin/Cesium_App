{
  "name": "cesium-app",
  "version": "1.0.0",
  "description": "Cesium application with 511NY camera integration",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev:express": "node server/server.js",
    "dev:netlify": "netlify dev",
    "build": "echo 'No build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^22.1.6"
  }
}
